import formConfigurationRoutes from "./formConfiguration.routes.js";

const formConfigurationModule =
    Object.freeze({
        routes:
            formConfigurationRoutes,
    });

export default formConfigurationModule;